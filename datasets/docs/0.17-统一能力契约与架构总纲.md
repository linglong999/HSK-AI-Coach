# 0.17 · 统一能力契约与架构总纲

> 本文档是架构"拧成一张图"的定稿篇。确认决策：
> **planner 自由 ReAct 为唯一对话入口；router 拆散收编；全部可交互能力收敛到统一 Skill 协议。**
> 与 0.16（生成引擎全流程）、agent-design.md M1–M9、JSON-接缝契约-v1 并列，作为后续开发的单一架构锚点。

---

## §0 本次架构反转（记录来龙去脉）

| 维度 | agent-design.md 设计（旧） | 实际代码现状（旧） | 本此定稿 |
| --- | --- | --- | --- |
| 对话主入口 | planner 自由 ReAct 唯一入口，router 收编 | **router 轻路由固定序**为主（serve/demo 都走它），planner 实现了没接 | ✅ **回到 doc 设计**：planner 唯一入口；router 拆散为自由技能 |
| router 角色 | 收编为模式 | 主入口 | 兼容遗留 runner（`/api/process` 继续可用，不进新叙事） |
| 能力组织 | Skill 协议统一 | 各模块 ad-hoc 接口 | ✅ 统一 Skill 协议收敛 |
| 防漂移 | Agent 不直接对话 | 轻路由确定性 | ✅ **自由在调度、约束在数据写**（见 §6） |

结论：代码在 M4 后从"planner 中心"趟到了"轻路由固定序"，`agent-design.md` 是"逐阶段追加"文档、未同步这次横跳，停在 M9 当完成度清单。本文档把反转正回来，并补上当初缺的"统一契约骨架"。

---

## §1 统一 Skill 契约（所有可交互能力）

每一个可交互能力 = 一个 Skill 对象。四段式结构化字段 + `run(context) -> result`。

```
metadata:
  name        # 稳定 slug，供 planner 引用
  version     # 走版本管理，升级引擎不改 name
  summary     # 一句话：做什么
  triggers    # 何时该用（触发条件）
  input       # 入参说明 + 示例
  output      # 返回结构说明
  guardrails  # 边界：何时不要用（防误选/防越界）
input_schema / output_schema   # 供 planner 校验与生成调用
run(context) -> result         # 真正执行对应引擎
to_manifest()                  # 渲染完整详情给 LLM（planner 决定调用时按需取）
```

- 引擎一律不改动；技能只做入参映射、出参封装、四段式描述与护栏。
- 零第三方依赖。

---

## §2 自由技能总清单（拆散后的能力全集）

> 记号：`[引擎]`=已实现并被技能包装；入参要点仅列最小可用面。

| # | 技能 name | 包引擎 | 入参要点 | guardrails（防误选/防越界） |
| --- | --- | --- | --- | --- |
| 1 | `identify_errors` | Recognizer | text, level, native_lang | 仅整句需找错时；空串不给 |
| 2 | `explain_error` | Explainer | fragment/偏误 | 已识别出的错才讲解；不衍生知识 |
| 3 | `verify_retell` | Verifier | key_points, restatement | key_points 是唯一覆盖点来源；不自己造覆盖集 |
| 4 | `lookup_knowledge_point` | 词表/知识点 | query/知识点名 | 查定义用；不查偏误 |
| 5 | `get_review_queue` | ErrorGraph | — | 拉低掌握/到期项；不写图谱 |
| 6 | `generate_unit` | GenerationEngine（新增） | unit_type, context | explain/practice 首发；写回默认关，practice 命中已确认偏误才开 |
| 7 | `retrieve_corpus` | engine/rag | query, top_k, kp_only, level | 答疑引用权威语料；不编造来源；未命中如实说 |
| 8 | `web_search` | tools | query, max_results | 未配 key→not_configured；结果仅供补充不照搬 |
| 9 | `parse_document` | tools | text/url | 文本/简单文档；复杂 PDF 留位 |
| 10 | `graph_snapshot` | ErrorGraph | — | 只读快照；供 planner/前端看"你错在哪" |

> **router 拆散语义**：原 `router.process` 的"识别→讲解→验证→图谱→复习队列"三引擎固定序，不再作为新叙事的主入口。识别/讲解/验证/复习各自是上述自由技能（#1/#2/#3/#5），由 planner 按需自由串联；确定性闭环作为技能组合由 planner 调度时自然复现。**不新建组合技能**（用户裁决：拆散成自由技能，不给 router 包一层壳）。
>
> **实际注册（代码现状）**：当前 `skills/` 已注册 **9 个**：#1-9（**#7 `retrieve_corpus` 已于 2026-09-01 随 0.18 接入项② 转正**，与 `lookup_knowledge_point` 共享同一 RAG 索引实例）；**#10 `graph_snapshot` 不经技能暴露**，由 `GET /api/graph` 直接提供，供前端渲染"你错在哪"。

---

## §3 planner = 唯一对话入口

自由 ReAct 循环（`planner/loop.py`，MAX_STEPS=6）：

```
system = list_all() 紧凑技能清单 + 全局规则
messages = [system] + history + [user(user_input)]
for step in range(6):
    raw = llm(messages)                       # LLM 输出 JSON 数组文本
    items = parse_json_array(raw)             # 自写 parser（OpenMAIC 式）
    for it: text→累积；action→REGISTRY.get(name).run(params)→tool_result 喂回
    终止：本轮有 text 且无待续 action → break
达 6 步 / 解析失败 → fallback（不崩）
```

### 对话 JSON 契约 v1（`/api/dialog` 出参，已实现）

```
{
  "dialog_version": "v1",
  "learner_id": "...",
  "conversation_id": "...",     // M5 接入（0.18 项①）：会话归属；同 id 复用延续上下文，不传=default
  "text": "……",              // 给用户的自然语回复
  "used_skills": ["identify_errors", ...],
  "steps": 3,
  "fallback": false,
  "trace": [ {name, params, ok, result}, ... ],  // 技能调用轨迹 → 前端转译为"学习成果卡"
  "degraded": [ {stage, reason, fatal}, ... ],   // 结构化降级
  "graph": {...},             // 图谱快照（前端省一次 GET）
}
```

> **M5 多轮记忆已接入（2026-09-01）**：`/api/dialog` 服务端 `LearnerMemory` 为多轮上下文唯一权威源——按 `conversation_id` 读 `to_llm_history()` 注入 planner，本轮 user + assistant（含 fallback 文案）追加落盘 `data/memory_<learner_id>.json`；前端透传 `history` 字段不再使用。详见 0.18 接入项①。

### 与接缝契约-v1 的关系
- 接缝契约 v1 是"单次纠错闭环"（`/api/process`）的确定性出参，**保留**供前端既有卡渲染。
- dialog 契约 v1 是"自由对话"的 planner 出参。两者不互斥：planner 内部用自由技能产生结果，最终 NC 自然语 + 技能轨迹（`trace` 供前端转译成果卡）。

---

## §4 router 收编说明（本轮不动端口）

- `engine/router.py` **不删除、不改**：`/api/process`（识别→讲解→验证→图谱）、`/api/verify`、`/api/graph` 继续用它，**保前端壳稳定**。
- 定位从"架构主入口"**降级为兼容遗留 runner**：新叙事（自由对话）走 planner。
- 端口不动、前端 `web/index.html` 不动（用户裁决：先保骨架）。

---

## §5 统一能力边界图（拧成一张图）

```
┌─ 出入口层 ─────────────────────────────────────────────────
│  serve.py (HTTP:8612)
│    /api/dialog  →  planner 自由 ReAct（唯一对话入口，新增）
│    /api/process →  router 兼容 runner（保留，前端在用，不进新叙事）
│    /api/verify  →  Verifier 复述验证
│    /api/generate→  GenerationEngine 费曼/练习单元
│    /api/graph   →  ErrorGraph 快照
└──────────┬────────────────────────────────
┌─ 编排层 ──────────────────────────────────────────────────
│  planner.loop(ReAct)  唯一决策点（选哪个技能/怎么答）
│  读 list_all → 自主选技能 → 串 action/text → 6 步内终止
└──────────┬────────────────────────────────
┌─ 能力层（统一 Skill 协议）───────────────────────────────
│  identify_errors · explain_error · verify_retell
│  lookup_knowledge_point · get_review_queue · generate_unit
│  retrieve_corpus · web_search · parse_document · graph_snapshot
│   ↑ planner 只认这一套接口；技能自带 guardrails 框边界
└──────────┬────────────────────────────────
┌─ 引擎/服务层 ─────────────────────────────────────────────
│  recognizer · explainer · verifier · generation(authoring/validator/generator)
│  rag 倒排检索 · learner_memory · writeback(两段式) · error_ledger
┌─ 工具层 ──────────────────────────────────────────────────
│  tools/call_tool（web_search/parse_document 生效；asr/tts 留位）
┌─ 数据层 ──────────────────────────────────────────────────
│  error_graph（唯一权威）· datasets 词表/知识点（+官方 xlsx）
│  memory/<learner_id>.json · data/*.json · 全 stdlib
```

垂直贯穿三条主线：
- **自由**：仅编排层 —— planner 自主决定调度。
- **约束**：数据写 —— 图谱唯一权威，写永远走 writeback 两段式 + 幂等键；技能 guardrails 框边界。
- **降级**：任一引擎/工具失败 → 结构化 `degraded[]`，绝不崩、绝不污染（零依赖许诺不变）。

---

## §6 防漂移边界（自由 ReAct 与确定性如何共存）

| 原则 | 落地 |
| --- | --- |
| **自由在调度** | planner 可自主选技能、自由回答、自由追问 |
| **约束在写** | 图谱写入仅经 `writeback`（疑似→确认两段式），幂等键防重复 |
| **权威不动** | 偏误图谱 = 唯一权威源；planner 只读它做复习/画像提醒 |
| **引擎不改** | 所有引擎只被技能包装，不改动 |
| **卡不乱长** | 前端卡片只能沿权威边（KP→关联→混淆→待复习）生长，无自由发散 |

"Agent 不直接对话"的旧表述，收窄为"A 不直接写权威数据"——planner 不直接落图谱，它就是纯选择器。

---

## §7 本轮作者分工 & 遗留

- **A（已拍板）**：planner 唯一入口 / router 拆散收编 / 本轮不动端口与前端。
- **B（本次已实现）**：本文档（架构总纲+契约）、`generate_unit` 自由技能、`/api/dialog`（planner 装配，共享 serve 图谱）。
- **C（验收）**：planner 自由对话能自主调技能并返回修正；拆散不丢确定性闭环能力；168 测试全绿；零依赖。
- **遗留（后续迭代，不越界）**：~~把 vocab/rag/graph 技能的 input/output schema 补齐供 planner 严格校验~~（✅ 已随 0.18 接入项④a 完成：9 技能 schema 齐备 + `validate_params` 调用前校验/安全纠正/失败喂回自纠）；~~eval 的 80% 达标实跑确认~~（✅ 已随 0.18 接入项④b 完成：2026-09-01 两轮真实实跑，轨迹命中 86.7%/80%、终止率 100%/100%、judge 达标 100%/93.3%，全项过 0.11 阈值）；前端壳继续打磨（卡片观感 / 低置信区分 / 错误提示友好化）。

## §8 验收标准（本文档 done 判据）

- [x] `planner.run` 可对偏误句自主调 `identify_errors`（+必要时 `explain_error`）并返回修正 —— 已实现，并经 `/api/dialog` 实测（注入假引擎，3 步闭环：识别→讲解→结语）
- [x] router 拆散后，确定性纠错闭环能力（`/api/process`）不丢、测试全绿 —— `/api/process` 保留，全量 **176** 测试通过
- [x] `/api/dialog` 存在且返回 dialog 契约 v1（text/used_skills/steps/fallback/trace + degraded/graph）
- [x] `generate_unit` 已作为自由技能注册，planner 可调用
- [x] 全程零第三方依赖