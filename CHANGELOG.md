# Changelog

本项目所有值得注意的改动都记录在此文件。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号对应仓库内的里程碑批次（`P0.x` / `0.x`）。每条改动标注所属类别与出处（提交短 SHA / 设计稿），供追溯"改了什么、为什么改"。

> 说明：本 Changelog 记录**改了什么、为什么**（面向开发者与贡献者）。想看"给老师和同学看的浅显版本演进说明"，见 `VERSION_NOTES_zh.md`；想看每个关键决策的完整推理，见 `docs/decisions/`。

---

## [0.25] - 2026-09-11 · OCR + 教材对齐 + 教学评测收口

### Added
- **OCR 进入纠错闭环（P0.7）**：接入 RapidOCR，图片/PDF → 文本 → 同一纠错链；OCR 字表纠错产物沉淀进 RAG 检索索引，回答带来源。首轮真实引擎实测采集 **68 组汉字误读校正对**并落盘 `datasets/ocr_corrections/`。（`ce07771` → `datasets/docs/0.7-生成单元schema-v1.md`、`reports/P0.7-OCR性能成本记录.md`）
- **教材对齐《HSK标准教程》1–4（P0.14）**：课次映射机制 + 查询接口，考纲点 → 教材课次映射经人工核验转正，落 `考纲点-教材课次-已核.csv`。（`a241dd9`）
- **教学 Judge 评测（tutor_quality）**：三层 rubric（程序断言 / LLM-judge / 红线）+ 19 组 case + 驱动脚本，回答"到底有没有教学效果"。（`2fa380f` → `datasets/eval/tutor_quality/`、`reports/eval_tutor_quality_2026-09-11.md`）

### Fixed
- **前端壳面板**：认知地图 / 复习 / 度量面板三块修复。（`4d2a8d5`）

### Changed
- **README 精简**：删除 Roadmap / 关键设计决策 / 解决什么问题三块长文，收敛为近期更新 + 能力导航；标题措辞与 OpenMAIC 差异化；清理失效锚点。（`75fab0a`）

---

## [0.19] - 2026-09-07 ~ 09-09 · 效果度量 + 收费闸门 + 数据确定性收口

这是数据层与"能否自证效果"大规模收口的一波，测试从 385 一路涨到 633。

### Added
- **效果度量闭环**：`engine/metrics.py` + 度量面板（续学率 / 通关率 / 满意度等 4 指标），对话结束 1–5 星自动回写，指标随使用更新；口径文档见 `reports/metrics_caliber.md`。（伴随 `P0.19 收口`）
- **游客闸门 + BYOK（P0.10）**：未配 Key 的访客额度（并发安全、跨天重置、`429` 引导），界面内直接加任意 OpenAI 兼容供应商并免重启切换。（`0086fa7`、`a44e117`）
- **CI 回归（P0.9）**：GitHub Actions 全量回归 + `.gitattributes` 锁定行尾 + 失败行经 `::error::` 暴露到 annotations。（`b40d9b8`、`c83eef9`、`7dc7bcd`、`cd268e8` 等）

### Changed
- **图谱数据层多维度（P0.2/P0.3/P0.4/P0.5）**：节点加 `error_kind / nature` 双维度；新增正向证据接口 `ingest_positive`；`priority` 分类型加权 + 化石化逻辑；数据迁移到新 schema。（`0bbfd7b`、`a858a0c`、`0fb2826`、`b5037bf`）
- **GF 等级口径唯一真源（P0.6）**：`engine/levels.py` 收拢为三等九级的唯一定义，消除多处散落口径。（`36128b7`）
- **真间隔重复（P0.17）**：自实现简化 FSRS 替换原调度，到期 / boost 筛选入复习队列。（`ef1a675` → `engine/scheduler/fsrs.py`）
- **韩语母语迁移规则（P0.11）**：签名解耦 + ko 四条规则 + 空 / 未知母语降级 en。（`a58c6f0`）
- **考纲人工审核转正（P0.15）**：评级调准 + 补语拆出 + 依赖边标签入库。（`7174480`、`e64cc3d`）
- **评测诚实化（P0.8）**：放弃 κ，改为 Krippendorff's α 双口径，补充 2×2 手算自验 + bootstrap CI；结论诚实判定"直接 IAA 不成立"。（`83c037e`、`015b8bb` → `reports/iaa_yaclc.md`）
- **Prompt 层与教学数据修订批（P0.19）**：recast 三档梯度、文案常量收敛、"惯犯→高频"、语言政策分级、识别 few-shot 修自相矛盾 + 低置信档、迁移规则收紧 + 把字句回避等。（`0086fa7` → `0.19-教学内容审查清单-复审意见.md`）

### Security
- 发布预检扫描密钥 / 备份文件（`*.bak*` 剔除并入 `.gitignore`），README 测试数、License、命令路径全部校准到真实。（原则 9 / 11，见 `datasets/docs/4.x-可复用原则与方法.md`）

---

## [0.22] - 2026-09-03 · 习得伙伴转向

> 战略转折：从"偏误纠错引擎"转向"让人愿意开口的陪练"。测试 385 全绿（原 335 + 新增 50）。

### Added
- **L1 母语迁移归因**：结构化迁移知识表（6 条 en，后扩 ko），独立"候选假设 + 待确认"，**不进确诊图谱**，讲解明示母语成因；宁漏勿错。（`e72aaa2` → `datasets/docs/0.22-习得伙伴转向-设计稿.md` 方向1）
- **生活场景对话**：5 个预置场景（餐厅 / 购物 / 问路 / 看病 / 旅行）+ `GET /api/scenarios` + `unit_type` 扩 `dialogue`，扩展进生成引擎。（方向2）
- **介入时机三档**：组合信号（主动求助 / 输出空 / 含义不清 / 连续错率）→ `none / light / block`，打断上限 `interrupt_cap` 默认 3，流利时静默记录、卡壳才出手。（方向3）
- **个性化栏**：回复风格六档（默认 / 专业严谨 / 亲和友善 / 高效务实 / 天马行空 / 启发引导）+ 称呼 / 人设 / 自定义指令 / 打断频率，`POST /api/profile` 持久化。（`a5196a9` 延续）

### Changed
- **对话主链重构**：`/api/dialog` 每轮预扫喂图谱 + 分档注入 `[Intervention]` 段，句子→图谱链不变，只改识别触发。（D3.4）

### Removed
- 移除 `data/` 下动态产物（被 gitignore），规则表 / 场景库作为分发资产落 `datasets/`。

---

## [0.21] - 2026-09-03 · 教学语言分层

- **英文壳 + 中文内容**：界面与讲解骨架英文（教<u>学</u>语言），例句与词汇用中文（内容载体）；`roles_en` 补英文角色，教学层真正英文化。（`d46a0a6` → `datasets/docs/0.21-教学语言分层.md`）

## [0.20] - 2026-09-02 · 模型密钥与模型选择（BYOK）

- 界面内配置任意 OpenAI 兼容供应商的 API Key，免重启切换；未配则有访客额度。（`069fced` → `datasets/docs/0.20-模型密钥与模型选择.md`）

## [0.19] - 2026-09-02 · 前端壳 v2 + Agent 化架构整合

- **前端 Explore 化改造**：对话画布 + 认知地图 + 复习队列（`web/index.html`，0 前端依赖，原生 JS+SVG）。（`069fced`）
- **Agent 化架构整合**：planner 自由 ReAct + 统一 Skill 注册表（9 技能）+ 三引擎 + 确定性图谱，M1–M9 接入清单全部收口。（`6da67cf` → `datasets/docs/0.18-M1-M9-接入清单.md`）
- **契约 v1**：前后端 JSON 接缝（`contract_version` + 4 条消费守则），后端接缝优先于界面，避免前端返工。（`datasets/docs/JSON-接缝契约-v1.md`）
- 移除公开文档自指痕迹，决策沉淀仅留内部设计稿。（`51622f3`）

---

## [0.1] - 2026-08-28 · MVP 费曼式中文偏误纠错引擎

- **初版发布**：费曼式复述检验、偏误图谱（确定性数据层）、零第三方依赖、黄金评测集；干净句误报确定性护栏压到 0%。（`6da67cf`）

---

## 常见问题

- **为什么没有 `Unreleased` 节？** 本仓库以里程碑批次收口，一个批次合入即定为一条版本；未并入批次的改动在提交历史可见。
- **"版本号"为什么是 `0.19 / 0.22` 这种？** 沿用仓库设计稿自身的里程碑编号（0.x 设计稿 / P0.x 交付批次 / M1–M10 老编号），不另造语义化版本号，保证与 `datasets/docs/` 一一对应。